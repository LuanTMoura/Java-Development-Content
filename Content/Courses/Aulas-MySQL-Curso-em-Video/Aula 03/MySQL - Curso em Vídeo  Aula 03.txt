MySQL - Curso em Vídeo  Aula 03

00.
Inside the program XAMPP, you start the services Apache and MySQL.

01.
Database are a range of tables and Tables are a range of registers;
Registers are a range of fields/columns (main characteristic of something on a table, like name, age, etc.);

02.
To create a new database in MSQL you digit:

	create database nameoftable;

03.
Semicolon indicates the end of a command in database;

04.
There are two forms to open a database and run a table (command create table),
you do a double click on a database already created or digit de command: 

	use databasename;

05.
To show a table structure, you use the command: 
	
	describe nameoftable;